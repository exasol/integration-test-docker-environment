name: Check bash scripts

on: [push]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: SCM Checkout
        uses: actions/checkout@v4

      - name: Run shellcheck
        run: ./scripts/build/shellcheck.sh
